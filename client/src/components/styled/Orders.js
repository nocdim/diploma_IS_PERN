import styled from "styled-components";

const RemoveOrderDiv = styled.div`
    display: flex;
    justify-content: center;

    button {
        width: 40px;
        height: 40px;
        border: none;
        outline: none;
        border-radius: 8px;
        background-color: #3a3055;
        color: white;
        cursor: pointer;
        font-size: 18px;
        font-weight: 500;
    }
`

export {
    RemoveOrderDiv
}